<UserControl x:Class="InventoryApp.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Monthly Sales Reports" FontSize="26" Style="{StaticResource HeadingText}"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker SelectedDate="{Binding StartDate}" Width="130" Background="#ffffff" Foreground="#111827" Margin="0,0,16,0"/>
                
                <TextBlock Text="To:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker SelectedDate="{Binding EndDate}" Width="130" Background="#ffffff" Foreground="#111827" Margin="0,0,16,0"/>

                <Button Content="Filter" Command="{Binding FilterCommand}" Style="{StaticResource PrimaryButton}" Padding="16,8"/>
            </StackPanel>
        </Grid>

        <!-- Summary Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#ffffff" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Sales Amount" Foreground="{StaticResource MutedTextBrush}" FontSize="13" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TotalSalesAmount, StringFormat={}₹ {0:N2}}" FontSize="28" FontWeight="Bold" Foreground="#10b981"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#ffffff" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Weight Sold" Foreground="{StaticResource MutedTextBrush}" FontSize="13" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TotalWeightSold, StringFormat={}{0:N4} g}" FontSize="28" FontWeight="Bold" Foreground="#6366f1"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main section: DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid ItemsSource="{Binding SalesData}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date"          Binding="{Binding Date, StringFormat=dd/MM/yyyy HH:mm}" Width="140"/>
                    <DataGridTextColumn Header="Customer"      Binding="{Binding CustomerName}" Width="160"/>
                    <DataGridTextColumn Header="Items Summary" Binding="{Binding ItemsSummary}" Width="*"/>
                    <DataGridTextColumn Header="Net Wt (g)"    Binding="{Binding TotalWeight, StringFormat={}{0:N4}}" Width="120"/>
                    <DataGridTextColumn Header="Amount"        Binding="{Binding TotalAmount, StringFormat={}₹{0:N2}}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
