<Window x:Class="InventoryApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:InventoryApp.ViewModels"
        Title="Inventory Management Manager" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <StackPanel Grid.Column="0" Background="#f4f4f4" VerticalAlignment="Top" Margin="10">
            <TextBlock Text="Inventory App" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />
            
            <Button Content="Dashboard" Command="{Binding ShowDashboardCommand}" 
                    Padding="10" Margin="0,0,0,10" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Left" />
            
            <Button Content="Inventory" Command="{Binding ShowInventoryCommand}" 
                    Padding="10" Margin="0,0,0,10" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Left" />
        </StackPanel>

        <!-- Main Content Area -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="20" />
    </Grid>
</Window>
