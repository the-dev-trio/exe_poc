<UserControl x:Class="InventoryApp.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource AppBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Customer Directory" FontSize="26" Style="{StaticResource HeadingText}"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{StaticResource MutedTextBrush}"/>
                <TextBox Width="260" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         Padding="8,6" FontSize="14" Tag="Search by Name or Phone..."/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid ItemsSource="{Binding CustomersView}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Customer Name"  Binding="{Binding Name}"   Width="*"/>
                    <DataGridTextColumn Header="Phone / Mobile" Binding="{Binding Mobile}" Width="160"/>
                    <DataGridTextColumn Header="Address"        Binding="{Binding Address}" Width="250"/>
                    <DataGridTextColumn Header="Last Purchase"  Binding="{Binding LastPurchaseDisplay}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
